{
  "name": "@kb/ember-select-light",
  "version": "2.0.6",
  "private": true,
  "repository": "",
  "license": "MIT",
  "author": "",
  "workspaces": [
    "ember-select-light",
    "test-app"
  ],
  "scripts": {
    "build": "yarn workspace @kb/ember-select-light run build",
    "lint": "yarn workspaces run lint",
    "lint:fix": "yarn workspaces run lint:fix",
    "prepare": "yarn build",
    "start": "concurrently 'npm:start:*' --restart-after 5000 --prefix-colors cyan,white,yellow",
    "start:addon": "yarn workspace @kb/ember-select-light run start",
    "start:test-app": "yarn workspace test-app run start",
    "test": "yarn workspaces run test"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "prettier": "^2.8.3"
  }
}
